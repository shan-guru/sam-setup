{
  "name": "aws-sam-exp",
  "version": "1.0.0",
  "description": "AWS SAM experiment with DynamoDB Local",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1691.0"
  }
}

